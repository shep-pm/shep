{
  "name": "web",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check-node.mjs && node --test scripts/verify-dogs-index.ts && node --test scripts/verify-heading-anchors.ts && node --test scripts/verify-prose-budget.ts && node --test scripts/verify-docs-nav.ts && astro build && pagefind --site dist && node scripts/verify-pagefind-index.mjs && node --test scripts/verify-fragment-targets.ts && node --test scripts/verify-stacked-table-labels.ts && node --test scripts/verify-callout-children.ts",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.2.10"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "pagefind": "^1.5.2",
    "typescript": "^6.0.3"
  }
}
