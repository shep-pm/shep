//! `shep import env`: reading a `.env` into shep's stores.
//!
//! [`parse`] is the grammar and holds no shep concepts.

// No caller yet: the verb that wires this in lands in a later task.
#[allow(dead_code)]
pub(crate) mod parse;
