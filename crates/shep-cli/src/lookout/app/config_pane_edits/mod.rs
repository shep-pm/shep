//! Editing a field in the config pane: filing an edit, undoing it, and the
//! batch that goes out on close.

mod confirm_field;
mod cycle_field;
mod dispatch_writes;
mod file_edit;
mod restore_default;
